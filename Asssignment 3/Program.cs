using System;
class DoTheMath
{
  /* public static void Main()
    {
        int numerator;
        int denominator;
        int intResult;
        float floatResult;
        int remainder;
       

        Console.WriteLine("Please enter the numerator?\n");
        numerator = int.Parse(Console.ReadLine());
        Console.WriteLine("Please enter the denominator?\n");
        denominator = int.Parse(Console.ReadLine());

        intResult = numerator / denominator;
        remainder = numerator % denominator;
        floatResult = (float)numerator/ (float)denominator;
     
        Console.WriteLine($"Integer division result = {intResult} with a remainder {remainder}\n ");
        Console.WriteLine($"Floating point division result = {floatResult}\n");
        Console.WriteLine($"The result as a mixed fraction is ${intResult}({remainder}/{denominator})");

    }*/
 
}